spring:
  cloud:
    consul:
      config:
        enabled: true
        prefix: config
        default-context: applicaton
        watch:
          delay: 1000
          enabled: true
        format: yaml
